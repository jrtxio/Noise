<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="NoiseBackendExample.StoryListPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:NoiseBackendExample"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Spacing="20">
            <TextBlock Text="Noise Backend Example" Style="{StaticResource TitleTextBlockStyle}" HorizontalAlignment="Center"/>
            <ProgressRing x:Name="LoadingRing" IsActive="True" />
            <TextBlock x:Name="StatusText" Text="Initializing..." HorizontalAlignment="Center"/>
            
            <ListView x:Name="StoryListView" MaxHeight="600" Width="800" SelectionMode="Single" ItemsSource="{x:Bind Stories}" ItemClick="StoryListView_ItemClick" IsItemClickEnabled="True">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Padding="10">
                            <TextBlock Text="{Binding Title}" Style="{StaticResource SubtitleTextBlockStyle}"/>
                            <TextBlock Text="{Binding Id}" Style="{StaticResource CaptionTextBlockStyle}" Foreground="Gray"/>
                        </StackPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackPanel>
    </Grid>
</Page>

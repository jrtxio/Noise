<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="NoiseBackendExample.StoryDetailPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:NoiseBackendExample"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Padding="20">
            <Button x:Name="BackButton" Content="Back" Click="BackButton_Click" Margin="0,0,0,10"/>
            <TextBlock x:Name="TitleTextBlock" Style="{StaticResource TitleTextBlockStyle}" TextWrapping="Wrap"/>
        </StackPanel>

        <ListView x:Name="CommentsListView" Grid.Row="1" ItemsSource="{x:Bind Comments}" SelectionMode="None">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <StackPanel Padding="10" Spacing="5">
                        <TextBlock Text="{Binding Author}" FontWeight="Bold" />
                        <TextBlock Text="{Binding Text}" TextWrapping="Wrap" />
                        <TextBlock Text="{Binding Timestamp}" Foreground="Gray" FontSize="12" />
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        
        <ProgressRing x:Name="LoadingRing" Grid.Row="1" IsActive="True" Visibility="Collapsed" HorizontalAlignment="Center" VerticalAlignment="Center"/>
    </Grid>
</Page>

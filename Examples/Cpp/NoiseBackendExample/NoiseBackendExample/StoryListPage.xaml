<Page
    x:Class="NoiseBackendExample.StoryListPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Padding="20">
            <TextBlock Text="Hacker News Top Stories" 
                       Style="{StaticResource TitleTextBlockStyle}"/>
            <ProgressRing x:Name="LoadingRing" 
                          IsActive="True" 
                          Margin="0,10,0,0"/>
        </StackPanel>

        <!-- Story List -->
        <ListView x:Name="StoryListView" 
                  Grid.Row="1"
                  ItemsSource="{x:Bind Stories}"
                  SelectionMode="Single"
                  ItemClick="StoryListView_ItemClick"
                  IsItemClickEnabled="True">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid Padding="12" BorderBrush="{ThemeResource SystemControlForegroundBaseLowBrush}" 
                          BorderThickness="0,0,0,1">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        
                        <TextBlock Grid.Column="0" 
                                   Text="{Binding Title}" 
                                   TextWrapping="Wrap"
                                   Style="{StaticResource BodyTextBlockStyle}"/>
                        
                        <TextBlock Grid.Column="1" 
                                   Text="{Binding CommentCount}" 
                                   Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}"
                                   Margin="12,0,0,0"
                                   VerticalAlignment="Center"/>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
